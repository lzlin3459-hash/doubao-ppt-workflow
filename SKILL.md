---
name: doubao-ppt-workflow
description: 从原始数据表/文档自动生成汇报类PPT的"先分析后制作"工作流。当用户提供表格、数据文档或业务素材并要求生成PPT（经营复盘、电商月报、调研汇报、产品宣传、周会汇报等）时使用；也适用于用户已有数据但不知如何组织成PPT、或需要把分析洞察转成演示稿的场景。核心流程：分析数据产出编号洞察 → 人工选定2-3个重点 → 按每页单论点生成PPT初稿 → 人工审核微调。
---

# 豆包工作流：PPT生成实战

## 概览

把数据表/文档变成汇报 PPT，按五步执行：输入材料 → 先分析后制作 → 人工定重点 → 生成PPT初稿 → 人工审核微调。AI 的角色是"数据分析师 + 设计师 + 文案"，人在关键决策点把关。**禁止跳过第 2 步直接生成 PPT。**

## 工作流总览

1. 输入材料
2. 先分析后制作（产出数据分析结果 + 编号洞察）
3. 人工定重点（决策点①）
4. 生成 PPT 初稿
5. 人工审核微调（决策点②）

## Step 1：输入材料

- 完整读取用户提供的全部表格/文档，弄清数据结构、字段与计算口径。
- 材料不足（缺数据、口径不明）时先向用户确认，不臆测、不编造。

## Step 2：先分析后制作（关键步骤，不可跳过）

- 读取数据后先做分析，产出"数据分析结果"（可用数据分析与可视化能力呈现关键指标与趋势）。
- 输出**编号洞察**：每条洞察 = 一个事实/问题 + 一条建议，并标注数据来源与计算口径。
- 向用户展示全部洞察，等待用户选定重点后再继续，**不要直接生成 PPT**。

## 决策点①：是否有明确观点

- 用户已有明确观点/重点 → 直接进入 Step 3，按用户指定方向执行。
- 用户没有观点 → 基于洞察给出 2-3 个候选重点方向（如商品货盘、付费投流、用户留存），请用户选择。

## Step 3：人工定重点

- 请用户从洞察中选择 2-3 个核心方向；用户可用"洞察4、5"这类编号直接引用。
- 重点确定后再生成，不要在生成中途大幅更换方向。

## Step 4：生成 PPT 初稿

- 生成 PPT 前先读取 ppt skill（飞书幻灯片）的 SKILL.md 并按其规范执行。
- 硬性要求：
  - 每页只讲一个论点，标题用结论式表述（如"低毛利大单品正在侵蚀利润"）。
  - 多用高级图表（趋势、占比、对比等），优先用可视化能力产出图表并嵌入页面。
  - 自动配结论与讲稿（写入备注页）。
- 所有数字保留来源标注，不得编造。

## Step 5：人工审核微调

- 逐页核对数据与来源，发现错误立即修正。
- 语气审核：把"危言耸听"的表述改温和（如"正在侵蚀利润"→"需要关注利润结构"），按汇报对象调整措辞。
- 若 AI 生成数据错误占比超过 10%，全稿复核。

## 决策点②：汇报对象

- 老板/领导（看重结论）→ 每页首句用结论式标题，弱化过程细节。
- 业内专家（在意数据）→ 保留交互图表、明细数据与口径说明。

## 硬性规则

- **数据来源**：每个数字必须来自输入材料或可复现计算，PPT 中标注来源；无来源的数字不写入。
- **数据安全**：涉密/敏感数据先脱敏再使用，主动提醒用户避免上传机密经营数据到第三方。
- **幻觉核查**：AI 产出的洞察与图表数字必须与原始数据一致，交付前人工逐页核对。
- **Prompt 基线**：生成指令复用本技能模板；关键输出存档（截图/文件），模型变更后重新验证效果。

## 复用场景

- 电商：运营月报（后台数据 → GMV/ROI 分析 → 周会 PPT）。
- 咨询：调研问卷数据 → 提炼发现 → 客户汇报初稿。
- 产品营销：产品参数 + 图片 → 宣传 PPT 或一页纸。
